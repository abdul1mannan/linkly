# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/linkly?schema=public"

# Optional: Direct connection for migrations (required for connection pooling services like Supabase, Neon)
DIRECT_URL="postgresql://username:password@localhost:5432/linkly?schema=public"

# Optional: Your production URL (used for OpenGraph metadata and social sharing)
# Set this to your actual domain when deploying
NEXT_PUBLIC_URL="http://localhost:3000"

